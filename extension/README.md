# AnvilHolo

VS Code extension: click-to-context UI for `context-slicer`.

## Dev
- `npm install`
- `npm run build`

The extension spawns `dist/context-slicer` from the workspace root (preferred), or falls back to `context-slicer` on PATH.
